# Public GitHub repositories require no token in the first release.
# Later protected write-back will use a narrowly scoped GitHub App,
# an authenticated admin session and webhook signature validation.
